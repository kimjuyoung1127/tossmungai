// pages/index.tsx
// This file serves as the entry point for the app (route: /)
// It renders the onboarding screen

import OnboardingScreen from './onboarding'; // This will now fail since onboarding.tsx was moved

export default OnboardingScreen;
